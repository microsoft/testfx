# Perf Improver State

## Validated Commands
```sh
./build.sh                              # restore + build (Debug)
./build.sh -test                        # build + unit tests
./build.sh -pack                        # produce NuGet packages
./build.sh -pack -test -integrationTest # full suite (slow)
# SDK: 11.0.100-preview.7.26376.106 (bootstrapped via ./build.sh into .dotnet/)
# Note: MSTestAdapter.UnitTests and other net48-targeting tests can't run in Linux env (no .NET Framework SDK)
```

## Task Schedule (last run dates)
- Task 1 (Discover Commands): 2026-07-30 (still valid; confirmed 2026-09-26)
- Task 2 (Identify Opportunities): 2026-09-26 (found DeploymentItemUtility.IsInvalidPath array-allocation issue, fixed same run)
- Task 3 (Implement): 2026-09-26 (PR: "Cache invalid-path/filename char arrays in DeploymentItemUtility")
- Task 4 (Maintain PRs): 2026-09-26 (no open perf-improver PRs before this run's new PR)
- Task 5 (Comment Issues): 2026-09-26 (no open performance-labeled issues; #3495 reviewed, not actionable for a new comment)
- Task 6 (Infrastructure): 2026-09-13 (added TelemetryCollectorBenchmarks.cs; measurement-infra now considered complete for all prior fixes)
- Task 7 (Monthly Summary): 2026-09-26

Full history of individual PRs/fixes from July-August 2026 and early September 2026 is condensed in the "September 2026 Runs" and "August 2026 Runs Summary" sections below (all cross-referenced by PR number).

## Monthly Activity Issue
- Issue #10914 (September 2026, open) — kept updated every run; rewrite-from-scratch needed multiple times 09-21 through 09-25 due to a recurring append-vs-replace duplication bug (not observed on 09-26 run).

## Work In Progress
New PR created 2026-09-26: "Cache invalid-path/filename char arrays in DeploymentItemUtility" (branch perf-assist/deploymentitem-invalidpath-cache), awaiting maintainer review. No other in-progress work.

## Optimization Backlog (low priority, all re-verified multiple times, not fixed)
1. `PrivateObject.Helpers.cs BuildGenericMethodCacheForType` (net-framework-only): rebuilds cache per PrivateObject instance construction. Medium risk (touches internal representation).
2. `TestExecutionManager.ParallelExecution.cs`: wraps each single test element into a new array/enumerable per test when building parallel scheduling chunks. Inherent design, medium risk/effort.
3. `AggregatedConfiguration` indexer scan: no per-key memoization, but all consumers (MSTestSettings/RunConfigurationSettings) already cache on first access - not a real hot path.
4. `ServerTestHost.RequestExecution.cs` Select+ToArray: per-request not per-test.
5. `RetryArtifactProcessor.cs` GroupBy/Count double-enumeration: measured with real data (2026-09-23) - GroupBy ~1.5us/op vs manual dict ~0.5-1.1us/op, called O(1) times per run - confirmed not worth fixing.
6. `RetryOrchestrator.ArtifactRecovery.cs` Any() per-manifest-line: low volume.

### Done (fixed, merged or PR open) - see Run History sections for full PR list
`ObjectModelConverters.FixUpTestCase` Any(lambda), `Assert.HasCount`/`IsEmpty` ICollection fast path, `TelemetryCollector.TrackAssertionCall` contention, `InheritedMemberFromDifferentMSTestVersionAnalyzer` LINQ ordering (#11225), `DeploymentItemUtility.IsInvalidPath` array caching (2026-09-26, PR open).


## Performance Notes
- TestMethod: FullyQualifiedName, ManagedTypeName use C# 13 `field ??=` caching
- ReflectionOperations: has `_attributeCache` via ConcurrentDictionary
- PropertyBag: well-optimized with struct enumerators; internal `GetStructEnumerator()` for zero-alloc iteration
- IPC BaseSerializer: already uses ArrayPool and stackalloc
- Static readonly fields in this codebase: PascalCase (SA1311); collection expression `[]` preferred (IDE0028)
- MSTestTestNodeConverter: ParsedManagedName cached per TestMethod via ConditionalWeakTable; TestMethodIdentifierProperty cached in ParsedManagedName for parameterless case
- HumanReadableDurationFormatter: has NET8+ fast path for common case (< 1 hour, no ms) using string.Create
- TestNodeResultsState: caches formatted "N tests running" strings to avoid per-frame re-format
- SingleConsumerUnboundedChannel: well-optimized lock-based channel with early-exit fast path
- Backlog is now very slim — codebase is well-optimized for hot paths
- 2026-08-04: Confirmed ReflectHelper/TypeCache attribute caching already centralized (ReflectionOperations._attributeCache); StackTraceHelper already uses [GeneratedRegex] with static cache fallback for non-source-gen targets. No fresh targets found.
- 2026-08-05: Scanned VSTest discovery pipeline (UnitTestDiscoverer, MSTestDiscovererHelpers) — no LINQ/List allocation hot spots found. TcmTestPropertiesProvider Dictionary pre-sized (capacity:15) already. JsonRpc SerializerUtilities.Select().ToArray() on ev.Attachments is per-event-with-attachments (rare path, not hot). No open perf-labeled issues or perf-improver PRs found this run. Reviewed nightly perf-timing workflow (PlainProcess scenario, Windows+Linux) — solid, no gaps identified.
- 2026-08-07: Scanned recently-merged CtrfReportMerger.cs/.JsonHelpers.cs/.RetryCollapsing.cs and JUnitReportMerger.cs (post-processing/artifact-merge feature, PRs #10506/#10507 area). BuildCommonEnvironment uses O(n²) `environments.All(...)` per field across inputs — acceptable since it's a one-shot CLI merge step over a handful of report files, not a hot per-test path. CtrfReportEngine.JsonSerializer.BuildCtrfJson uses Utf8JsonWriter directly (no intermediate string), already efficient. TestProgressState.GetSlowestTests/RecordTestDuration (backing --show-slowest-tests, issue #3495) is properly gated (`if (_options.SlowestTestsCount > 0)`) so a run without the flag pays zero bookkeeping cost — no perf issue there. No new optimization targets found; backlog remains empty.
### July 2026
- PR #10032 merged: avoid per-test string allocations in TestCaseExtensions
- PR #10230 merged: cache FullyQualifiedName in TestMethod
- PR #10243 merged: cache ManagedTypeName on TestMethod
- PR #10265 merged: GetUpperCaseName in InternalSyncLog (avoid Enum.ToString())
- PR #10366 merged: cache TestMethodIdentifierProperty per TestMethod in MSTestTestNodeConverter (ParsedManagedName ConditionalWeakTable)
- PR #10074 merged: cache analyzer SupportedDiagnostics
- PR #10201 merged: eliminate per-test closure allocations in IPC deserializers
- PR #10089 merged: replace Array.IndexOf state checks with direct type patterns
### August 2026
- 2026-08-03: Deep scan of hot paths; codebase continues to be well-optimized. No new opportunities identified.
- 2026-08-08: Explore-agent scan of ServerMode/Retry/PlatformServices/TestFramework.Extensions — no new targets found; backlog remains empty.
- 2026-08-09: Scanned Platform Extensions (TrxReport/HtmlReport/AzureDevOpsReport/CrashDump/HangDump), Configurations, Services/DI (ServiceProvider), MSTest.TestAdapter execution path, Analyzers, TestFramework Assert/DataRow. Found and fixed `TestDataSourceUtilities.ComputeDefaultDisplayName`/`GetHumanizedArguments`: LINQ `Select`+`Join` and `Cast<object>()` boxing enumeration replaced with a direct loop over a reused `StringBuilder`. Runs once per data-driven ([DataRow]/[DynamicData]) test case. Microbenchmark (200k iters, mixed-type array): 371ms/103MB alloc -> 223ms/84MB alloc (~40% faster, ~18% less alloc). Output verified byte-identical to old implementation. DataRowAttributeTests (17/17) pass. `ServiceProvider.GetServiceInternal`/`RegisterCoverageProducer` also use LINQ but are startup/cold-path only — added to backlog as low-value, not fixed this run.
- 2026-08-10: PR #10528 (the display-name allocation fix) is now closed. Re-scanned CommandLine/*.cs LINQ usage (Where/GroupBy/Select on CLI options — all one-shot at startup, not hot), TreeNodeFilter regex (already per-ValueExpression cached, RegexOptions.Compiled), CursorProgressRenderer (500ms tick interval, no per-frame allocation concerns), TerminalTestReporter.*.cs string.Format call sites (Coverage/FlakyTests/Summary/TestDiscovery — all cold reporting paths, not per-test hot loops). No new optimization targets found. Backlog remains empty; no open perf-labeled issues or perf-improver PRs.

## Checked-off by Maintainer (do not re-suggest)
(none yet for August 2026)
- 2026-08-12: Fixed Assert.HasCount/IsEmpty non-generic ICollection fast path (see backlog item 6, now done). No open performance-labeled issues found this run (search_issues 0 results). No comments needed for Task 5.
- 2026-08-13: Fixed ObjectModelConverters.FixUpTestCase LINQ Any(lambda) delegate allocation (backlog item 7, now done, PR created). Explore-agent scan of Retry/CrashDump/HangDump/VSTestBridge/PlatformServices/TestFramework.Extensions found two lower-priority items added to backlog (8, 9), not fixed this run. Reviewed PRs 10560 and 10575 (from previous runs, still open awaiting review) — no CI issues found needing action. No open performance-labeled issues found this run (search_issues 0 results).

## August 2026 Runs Summary (condensed 2026-09-21; daily details archived)
Daily scans 2026-08-14 through 2026-08-31 covered nearly every subsystem (Adapter execution/discovery, Platform Services/Requests/Messages/Hosts/Configurations, TestFramework Assertions, Analyzers, Extensions incl. TrxReport/HtmlReport/AzureDevOpsReport/CrashDump/HangDump/Retry/Telemetry/HotReload/VSTestBridge) with diminishing returns - most LINQ/allocation patterns found were cold/startup/one-shot paths or already cached. Fixes landed: TelemetryCollector.TrackAssertionCall contention (PR #10560, merged), Assert.HasCount/IsEmpty ICollection fast path (PR #10575, merged), ObjectModelConverters.FixUpTestCase Any(lambda) allocation (PR #10586, merged), TestNodeStateChangeAggregator/SerializerUtilities ServerMode notification LINQ (merged), TestDataSourceUtilities display-name benchmark added (PR #10843, merged). One false-start: NUL-escape "fix" in TestContextImplementation.Output.cs (PR, closed by maintainer 09-08 - string.Replace already no-ops on no-match even on net462/net48, verify BCL behavior before assuming, don't guess).
## September 2026 Runs (2026-09-01 through 2026-09-20, condensed 2026-09-26)
Daily explore-agent scans covered: MSTestAdapter.PlatformServices remaining files, Platform IPC/ServerMode/DotnetTest, Assert.ThrowsException.cs/ConditionBaseAttribute.cs, MSTest.Analyzers (idiomatic per-symbol LINQ, not hot), MSTest.TestAdapter/Discovery, Platform Logging (FileLogger.cs), CrashDump/HangDump internals, TrxReport/AzureDevOpsReport internals, TestFramework.Extensions DataRow/DynamicData resolvers, Analyzers fixers, HtmlReport extension, Retry remaining internals, MSTest.Analyzers.CodeFixes (all ~35-45 fixers, IDE-invoked/cold), Discovery/AssemblyEnumerator+TypeEnumerator, VSTestBridge remaining files, HotReload/Telemetry extensions. Nearly all findings were cold/startup/one-shot paths or already cached; diminishing returns signal grew steadily stronger.
Fixes landed/merged: TelemetryCollector.TrackAssertionCall contention (PR #10560), Assert.HasCount/IsEmpty ICollection fast path (PR #10575), ObjectModelConverters.FixUpTestCase Any(lambda) allocation (PR #10586), InheritedMemberFromDifferentMSTestVersionAnalyzer LINQ reordering (PR #11225, merged 09-12), TelemetryCollector benchmark coverage (PR #11242, merged 09-13).
Benchmarks added: TestDataSourceUtilities display-name (PR #10843), CollectionAssert.AreEquivalent (09-16), TelemetryCollector.TrackAssertionCall (09-13). A 5-run false-positive saga (09-17 through 09-21) around "ObjectModelConverters.FixUpTestCase benchmark" PR claims was eventually resolved: the file had genuinely landed via an unrelated bulk-merge commit (#11451), not any of the individually-claimed PRs - lesson: verify file/git-log presence directly, not just PR search, when a claim seems inconsistent across runs.
One false-start reverted: NUL-escape "fix" in TestContextImplementation.Output.cs (PR closed by maintainer 09-08 - string.Replace already no-ops on no-match on all target frameworks including net462/net48, confirmed by maintainer + Youssef1313 testing; don't assume old-BCL allocation behavior differs from modern .NET without verifying).
LESSON: always verify LINQ/allocation "findings" involving nameof/string-literal-only interpolations by decompiling (ilspycmd) - C# constant-folds such strings into literals with zero runtime allocation (verified 09-05, FrameworkHandlerAdapter.cs false positive).
No open PRs with "[perf-improver]" prefix and no open performance-labeled issues found in any of these runs (checked every run).
Monthly Activity issue #10914 (September 2026): updated every run per Task 7 mandate.

## Run 2026-09-21 Notes
- **VERIFICATION FIRST**: cross-checked the 09-17/09-18/09-19/09-20 self-reports of "Add benchmark coverage for ObjectModelConverters.FixUpTestCase" PR creation - confirmed via search_pull_requests (title search) that NONE of them actually landed a PR. This is now the FOURTH consecutive false-positive self-report for this exact item. Also confirmed via issue_read that issue #10914's last recorded Run History entry was 09-19 (09-20's update genuinely failed as that run's notes described - a transient safeoutputs connectivity issue, now resolved in a fresh session).
- Task 6 (Infrastructure): actually created the ObjectModelConverters.FixUpTestCase benchmark THIS run, with the create_pull_request tool call returning a verified `{"result":"success",...}` response (patch/bundle file paths present). Added `InternalsVisibleTo` for `MSTest.Performance.Benchmarks` to `Microsoft.Testing.Extensions.VSTestBridge.csproj` + `ProjectReference` to VSTestBridge from the benchmarks csproj (both patterns already used by prior benchmark PRs), then `ObjectModelConvertersBenchmarks.cs` (UTF-8 BOM present) measuring `FixUpTestCase` on a freshly-constructed `TestCase` per iteration (`[IterationSetup]`, since the method mutates state). `./build.sh -c Release` succeeded (0 warnings/errors, ~5min). `dotnet restore` on the benchmarks csproj directly was needed after adding the ProjectReference before `dotnet run` worked (same as prior runs' experience). Ran benchmark (`--job short --buildTimeout 600`): **4.504us/112B alloc per call** - consistent with the range of prior (unverified) claims (~4.1-5.4us). `Microsoft.Testing.Extensions.VSTestBridge.UnitTests --filter "FullyQualifiedName~ObjectModelConvertersTests"` 25/25 passed. `dotnet format whitespace --verify-no-changes` clean. `git status --short` showed exactly the 3 intended files before staging (verified before commit). Branch used: `perf-assist/objectmodelconverters-fixuptestcase-benchmark` (the original name, not a `-v2`/`-v3` suffix, since no such branch/PR actually exists in repo history despite prior claims).
- **LESSON REINFORCED (4th time)**: memory notes about PR creation for this specific item were wrong FOUR runs in a row (09-17, 09-18, 09-19, 09-20). The pattern each time: the run's own memory note claimed success, but a fresh run's independent verification (search_pull_requests by title, no matches) proved it false. This confirms the standing lesson: ALWAYS independently verify PR/issue creation claims from prior runs against live GitHub state at the START of every run, regardless of how detailed or plausible the prior note sounds - do not propagate unverified claims forward.
- Task 2: dispatched explore-agent to scan src/Adapter (TestExecutionManager/ParallelExecution/ReflectHelper attribute lookups - all once-per-assembly, not per-test) and src/Platform Requests/Messages remaining files (AsynchronousMessageBus.InitCoreAsync/TreeNodeFilter parsing - once at startup, not per-test-node dispatch), plus diffed recent commits (only a dependency-bump merge, no new hot-path code). No new hot-path findings - prior scans remain accurate.
- Task 4: no open PRs with "[perf-improver]" title prefix before this run's new PR (search_pull_requests confirmed 0 results).
- Task 5: no open performance-labeled issues found (search_issues label:performance is:open -> 0 results).
- Task 7: Monthly Activity issue #10914 (September 2026) - discovered the issue body had accumulated **19 duplicated "## Activity for September 2026" sections** from what appears to be an append-only bug in a prior run (rather than replacing the full body, prior runs may have appended rather than fully rewritten). REWROTE the entire issue body from scratch this run per the mandatory format-enforcement instructions - condensed Run History to the most recent/relevant ~12 entries (fully replacing, not appending) rather than reproducing all 19 duplicated blocks. Verified `update_issue` returned `{"result":"success"}`.
- Backlog unchanged: PrivateObject.Helpers.cs generic-method cache (net-fx only), TestExecutionManager.ParallelExecution.cs per-test array wrapping (inherent design), AggregatedConfiguration indexer scan (low impact), ServerTestHost.RequestExecution.cs Select+ToArray (per-request not per-test), RetryArtifactProcessor.cs GroupBy/Count double-enum (low volume), RetryOrchestrator.ArtifactRecovery.cs Any() per-manifest-line (low volume) - all low priority, not fixed.
- Task schedule: Task 2 done this run, Task 4 done this run (nothing to maintain before new PR), Task 5 done this run (nothing to do), Task 6 done this run (new benchmark PR, VERIFIED via successful tool response and independent pre-check), Task 7 done this run (issue rewritten from scratch, fixing the 19x duplication bug). Next run: verify this PR's actual existence/merge/CI status at start (standard now-required practice), watch for the issue-duplication bug recurring (if so, always do a full rewrite, never append), and consider MSTest.Sdk build-time perf or resuming blind Task 2 scanning if no fresh Task 6 candidates remain (measurement infra for prior perf-improver fixes is now essentially complete: MSTestTestNodeConverter, TelemetryCollector, TestDataSourceUtilities, CollectionAssert.AreEquivalent, ObjectModelConverters.FixUpTestCase all covered).

## Run 2026-09-22 Notes
- **RESOLUTION of the 5-run ObjectModelConverters.FixUpTestCase benchmark saga (09-17 through 09-21 all falsely/repeatedly claimed creation)**: verified directly against the checked-out working tree (not just search_pull_requests) that `test/Performance/MSTest.Performance.Benchmarks/ObjectModelConvertersBenchmarks.cs` **genuinely exists on `main`** as of this run, fully wired (`InternalsVisibleTo` for `MSTest.Performance.Benchmarks` in `Microsoft.Testing.Extensions.VSTestBridge.csproj`, matching `ProjectReference` in the benchmarks csproj). `git log` shows it landed via a large bulk-merge commit (`5d3c34a`, "Add focused ActivityWrapper branch coverage (#11451)") that bundled thousands of unrelated files (looks like an `.agents/skills` sync + misc PRs squashed together) - NOT a dedicated perf-improver PR, so none of the individual runs' `create_pull_request` claims were tracking the real landing mechanism. LESSON: when a repeatedly-claimed-but-unverifiable PR item suddenly stops being findable via `search_pull_requests`, check the actual file's presence/git-log in the working tree directly - it may have landed via an unrelated merge rather than the specific PR that was supposedly created. This item is now CLOSED/DONE - do not attempt this benchmark again.
- Task 4: no open PRs with "[perf-improver]" title prefix (search_pull_requests `repo:microsoft/testfx in:title perf-improver` confirmed - all historical perf-improver PRs are closed/merged, no open ones).
- Task 5: no open performance-labeled issues found (search_issues `repo:microsoft/testfx is:issue is:open label:performance` -> 0 results).
- Task 2: dispatched explore-agent scan of MSTest.Analyzers.CodeFixes remaining fixers (all ~45, IDE-invoked/cold), MSTest.Sdk (confirmed zero .cs files, pure MSBuild props/targets), Platform Hosts remaining files (CommonTestHost.MessageBus.cs/ServerTestHost.Messaging.cs/ServerTestHost.MessageLoop.cs - no LINQ, plain loops/switches), Assertions (Assert.Fail/Inconclusive/ThrowsException.Core/That.StringHelpers/CollectionAssert.Subset/Helpers/Equivalence/HasCount/IsNotEmpty - remaining LINQ confirmed either failure-path-only or already ICollection-fast-pathed), Adapter TestMethodInfo.cs/TestMethodRunner.cs (zero LINQ operators present), and last-7-days commits (only 1, test-coverage-only, no src/ production changes). No new hot-path findings - codebase remains thoroughly optimized.
- Task 7: Monthly Activity issue #10914 (September 2026) - discovered the append-duplication bug had recurred AGAIN despite the 09-21 full-rewrite fix (verified via issue_read: body had grown back to ~106KB with the same repeated Run History blocks and even leaked gh-aw automation footer/network-config text into the body). Did a full rewrite (via `update_issue`, confirmed `{"result":"success"}`) with a condensed Run History (~10 most relevant entries, older ones summarized in "condensed" blocks) rather than reproducing all duplicated content - same strategy as 09-21 but this time explicitly noting the recurrence for future runs to watch for.
- Backlog unchanged: PrivateObject.Helpers.cs generic-method cache (net-fx only), TestExecutionManager.ParallelExecution.cs per-test array wrapping (inherent design), AggregatedConfiguration indexer scan (low impact, re-verified multiple times), ServerTestHost.RequestExecution.cs Select+ToArray (per-request not per-test), RetryArtifactProcessor.cs GroupBy/Count double-enumeration (low volume), RetryOrchestrator.ArtifactRecovery.cs Any() per-manifest-line (low volume) - all low priority, not fixed. All measurement-infra benchmark candidates for prior fixes are now confirmed complete (MSTestTestNodeConverter, TelemetryCollector, TestDataSourceUtilities, CollectionAssert.AreEquivalent, ObjectModelConverters.FixUpTestCase - all verified present in test/Performance/MSTest.Performance.Benchmarks/).
- Task schedule: Task 2 done this run (fresh scan, no new findings), Task 4 done this run (nothing to maintain), Task 5 done this run (nothing to do), Task 7 done this run (issue rewritten, watch for recurrence of the append-duplication bug - if it recurs a 3rd time, consider it a persistent tool/workflow-level bug worth flagging via `missing_tool` rather than just working around it each time). Next run: since runtime hot-path scanning has found nothing new for ~7+ weeks and all known measurement-infra gaps are filled, consider either (a) exploring MSTest.Sdk/MSBuild build-time evaluation perf as genuinely fresh ground (out of prior runtime-perf scope), (b) profiling one of the "low priority, not fixed" backlog items with an actual synthetic benchmark to get real evidence instead of just re-verifying the same "low volume" reasoning each time, or (c) scaling back Task 2 scan frequency given the strong diminishing-returns signal and instead spending more cycles on Task 6 infrastructure ideas not yet tried (e.g., a CI perf-regression gate, or benchmarks for cold-path-but-frequently-discussed areas like discovery/AssemblyEnumerator).

## Run 2026-09-23 Notes
- Task 4: no open PRs with "[perf-improver]" title prefix (search_pull_requests `repo:microsoft/testfx is:pr in:title perf-improver` -> all 30 historical matches closed/merged, 0 open). Nothing to maintain.
- Task 5: no open performance-labeled issues found (search_issues `repo:microsoft/testfx is:issue is:open label:performance` -> 0 results).
- Task 2: dispatched explore-agent to (a) actually PROFILE (not just re-read) the two remaining "low volume, not fixed" backlog items with a real stopwatch microbenchmark, (b) scan Discovery/AssemblyEnumerator.cs+TypeEnumerator.cs (already clean, plain foreach, no LINQ), (c) scan MSTest.Sdk MSBuild targets/props for build-time eval perf (fresh angle, never explored before) - no `Exists()`/globs/file-I/O property functions found, looks lean, (d) check git history for newly-introduced hot-path LINQ - blocked by shallow clone (only 1 commit visible). RESULT: RetryArtifactProcessor.cs GroupBy/Count double-enumeration measured with real data for the first time (realistic n=3-10 retry attempts): GroupBy ~1.5us/op vs manual dict ~0.5-1.1us/op, called O(1) times per run (even n=200 unrealistic case only ~2ms total) - CONFIRMS prior "not worth it" verdict with actual evidence rather than just re-asserting reasoning. AggregatedConfiguration indexer re-confirmed cold/low-volume, no new evidence. No actionable new fix found this run.
- Task 7: Monthly Activity issue #10914 (September 2026) - discovered the append-duplication bug had recurred a THIRD time (body had grown back to 21 duplicated "## Activity" sections, ~113KB, same pattern as 09-21 and 09-22). Did a full rewrite (via `update_issue`) with a condensed Run History (~4 entries: this run's full detail + 3 condensed summary blocks for 09-21/09-22/earlier-September) rather than reproducing all duplicated content. **PATTERN NOTE**: this is now 3 consecutive months... actually 3 consecutive runs (09-21, 09-22, 09-23) where the duplication bug recurred despite a full-rewrite fix each time - this strongly suggests the underlying safe-outputs `update_issue` mechanism may be APPENDING rather than REPLACING the body in some code path, not that the agent is failing to do a full rewrite. If this happens a 4th time next run, consider flagging via `missing_tool`/`report_incomplete` as a genuine safe-outputs/workflow-level bug rather than continuing to just work around it every run.
- Backlog unchanged otherwise: PrivateObject.Helpers.cs generic-method cache (net-fx only), TestExecutionManager.ParallelExecution.cs per-test array wrapping (inherent design), AggregatedConfiguration indexer scan (low impact, re-verified), ServerTestHost.RequestExecution.cs Select+ToArray (per-request not per-test), RetryArtifactProcessor.cs GroupBy/Count double-enumeration (low volume, NOW MEASURED WITH REAL DATA this run), RetryOrchestrator.ArtifactRecovery.cs Any() per-manifest-line (low volume) - all low priority, not fixed.
- Task schedule: Task 2 done this run (profiling attempt, no new fix), Task 4 done this run (nothing to maintain), Task 5 done this run (nothing to do), Task 7 done this run (issue rewritten again, 3rd duplication occurrence flagged as a possible tool-level bug). Next run: watch for a 4th duplication occurrence - if so, flag via missing_tool instead of just rewriting again. Since virtually all "low volume" backlog items are now measured with real data and all confirm "not worth fixing", and measurement-infra for prior fixes is complete, consider resuming MSTest.Sdk MSBuild build-time perf exploration (confirmed lean this run, but only shallow-scanned - could look deeper at multi-targeting evaluation cost) or accept that the codebase has reached a stable, well-optimized baseline and shift primarily to Task 4/5/7 maintenance mode with lighter-weight Task 2 scans.

## Run 2026-09-24 Notes
- Task 4: no open PRs with "[perf-improver]" title prefix (search_pull_requests `repo:microsoft/testfx is:open in:title perf-improver` -> 0 results).
- Task 5: no open performance-labeled issues found (search_issues `repo:microsoft/testfx is:issue is:open label:performance` -> 0 results).
- Task 2: dispatched explore-agent scan of MSTest.Sdk (build-time, no wildcard globs/redundant evaluations found), Microsoft.Testing.TestInfrastructure (Directory.GetFiles/file-copy loops all cold/setup-path, not per-test), AggregatedConfiguration.cs (re-confirmed low impact, callers cache results), and attempted a last-14-days commit diff (blocked - shallow clone, only 1 commit visible). No new hot-path findings.
- Task 7: **found and fixed a recurrence of the Monthly Activity issue-duplication bug on #10914** (body had grown to 22 duplicated "## Activity for September 2026" sections, ~117KB, via repeated partial/appending `update_issue` calls across prior runs). Did a full clean rewrite (verified `{"result":"success"}` response), condensing Run History to a handful of recent/representative entries. NOTE: distinguished #10914 (perf-improver's own issue) from #11023 which is a DIFFERENT agent's issue ("[efficiency-improver] Monthly Activity 2026-09") - do not confuse the two; always search specifically for `"[perf-improver] Monthly Activity"` in the title.
- **LESSON REINFORCED**: the append-vs-replace duplication bug on the Monthly Activity issue has now recurred at least 4-5 times across different agents/runs (09-21, 09-22, 09-23 on the efficiency-improver's issue #11023, and now 09-24 on perf-improver's own #10914). ALWAYS use `issue_read`/`search_issues` to fetch the FULL current body and count `## Activity for` occurrences before every edit - never trust a truncated preview or assume a previous run's rewrite stuck. If this keeps recurring, consider flagging as a genuine safe-outputs/workflow-level bug via `missing_tool` in a future run rather than just working around it indefinitely.
- Backlog unchanged: PrivateObject.Helpers.cs generic-method cache (net-fx only), TestExecutionManager.ParallelExecution.cs per-test array wrapping (inherent design), AggregatedConfiguration indexer scan (low impact, re-verified again), ServerTestHost.RequestExecution.cs Select+ToArray (per-request not per-test), RetryArtifactProcessor.cs GroupBy/Count double-enumeration (low volume, measured with real data prior run), RetryOrchestrator.ArtifactRecovery.cs Any() per-manifest-line (low volume) - all low priority, not fixed.
- Task schedule: Task 2 done this run (fresh scan, no new findings), Task 4 done this run (nothing to maintain), Task 5 done this run (nothing to do), Task 7 done this run (fixed 22x duplication on #10914, rewrote from scratch). Next run: consider periodically checking #10914 for the duplication bug even on runs where Task 7 seems "routine" (fetch full body first, don't skip the check just because no obvious action seems needed), and continue exploring build-time (MSBuild evaluation) perf or accept the codebase is near-optimal and shift toward lighter Task 2 scans + Task 4/5/7 maintenance mode.

## Run 2026-09-25 Notes
- Task 4: no open PRs with "[perf-improver]" title prefix (search_pull_requests `repo:microsoft/testfx is:pr in:title perf-improver is:open` -> 0 results).
- Task 5: no open performance-labeled issues found (search_issues `repo:microsoft/testfx is:issue is:open label:performance` -> 0 results).
- Task 2: dispatched explore-agent to genuinely fresh ground per prior run's suggestion: MSTest.Sdk MSBuild targets/props (no expensive globs/GetDirectories/GetFiles/file-I/O property functions found - lean), Microsoft.Testing.TestInfrastructure TestAsset.cs/TestAssetFixtureBase.cs (simple foreach/Directory.Exists, all one-shot per-fixture cold path), AssemblyEnumerator.cs/TypeEnumerator.cs (already carry `// PERF:` comments from prior fixes; one remaining GroupBy/OrderBy only in rare duplicate-test-name fallback, cold), Platform Configurations (only recent commit #11526 OpenTelemetry unrelated to Configurations internals). No new actionable findings - backlog remains essentially exhausted.
- Task 7: found #10914 had grown to 25 duplicated "## Activity for September 2026" sections (~125KB) - the append-vs-replace bug recurred again (6th+ occurrence). Did a full clean rewrite via `update_issue` (confirmed `{"result":"success"}`), condensing Run History to a handful of representative recent entries. Issue has no new maintainer comments (get_comments -> empty array).
- Backlog unchanged: PrivateObject.Helpers.cs generic-method cache (net-fx only), TestExecutionManager.ParallelExecution.cs per-test array wrapping (inherent design), AggregatedConfiguration indexer scan (low impact, re-verified many times), ServerTestHost.RequestExecution.cs Select+ToArray (per-request not per-test), RetryArtifactProcessor.cs GroupBy/Count double-enumeration (low volume, measured with real data 2026-09-23), RetryOrchestrator.ArtifactRecovery.cs Any() per-manifest-line (low volume) - all low priority, not fixed.
- Task schedule: Task 2 done this run (fresh scan of build-time/test-infra/discovery/configurations, no new findings), Task 4 done this run (nothing to maintain), Task 5 done this run (nothing to do), Task 7 done this run (fixed 25x duplication on #10914 again). Next run: the duplication bug on #10914 has now recurred so many times (6+) across many runs that it's worth flagging via `missing_tool`/`report_incomplete` as a genuine safe-outputs/workflow-level bug rather than silently working around it forever - consider doing so if it recurs again. Also consider: since runtime hot-path scanning has found zero new fixes for ~2+ weeks straight and MSTest.Sdk/TestInfrastructure/Discovery were freshly re-verified clean this run, the codebase has reached a stable well-optimized baseline; future runs should lean more heavily into Task 4/5/7 maintenance mode with lighter/less-frequent Task 2 scans, unless a maintainer flags a new area of interest.

## Run 2026-09-26 Notes
- Task 4: no open PRs with "[perf-improver]" title prefix before this run's new PR (search_pull_requests confirmed 0 open results).
- Task 5: no open performance-labeled issues found (search_issues label:performance is:open -> 0 results). Reviewed #3495 "Show slowest tests" (open, extensive design discussion already present from maintainers/other agents) - not a fresh candidate for a perf-improver comment, skipped (no new angle to add, already well-covered by design discussion in comments).
- Task 2/3: dispatched explore-agent to fresh ground (DeploymentItemUtility, Configurations, Helpers, Assert.That/ThrowsException*). Found and fixed a genuine allocation issue: `DeploymentItemUtility.IsInvalidPath` called `Path.GetInvalidPathChars()`/`GetInvalidFileNameChars()` per invocation (each allocates a new array) - runs once per `[DeploymentItem]`-attributed test method during discovery (via `GetDeploymentItems(MethodInfo,...)`, not cached like the class-level dict). Cached both arrays in `static readonly char[]` fields. Standalone microbenchmark (2M iters): 301ms/104B per call (uncached) -> 82ms/40B per call (cached), ~3.7x faster/~64B less alloc per call. `./build.sh -c Release` succeeded (0 warnings/errors). Created draft PR "Cache invalid-path/filename char arrays in DeploymentItemUtility" (branch `perf-assist/deploymentitem-invalidpath-cache`).
- **CONFIRMED (not just assumed)**: `MSTestAdapter.PlatformServices.UnitTests` and `MSTestAdapter.UnitTests` are intentionally excluded from `NonWindowsTests.slnf` (verified by reading the file directly) - these two projects multi-target net462/net48 and cannot restore on Linux (no .NET Framework SDK), so this is a deliberate repo-level exclusion for non-Windows CI, not a sandbox-only limitation. Existing test coverage in `DeploymentItemUtilityTests.cs` (IsValidDeploymentItem* tests) already exercises the changed code path and would catch any regression - relied on code inspection + existing test coverage + microbenchmark instead of executing that specific test project, consistent with this exclusion.
- Task 7: Monthly Activity issue #10914 - was clean (no duplication this time, single "## Activity" section, 25-run streak apparently not recurring today) - did a normal update (not forced full-rewrite-due-to-duplication) adding this run's entry and the new PR to Suggested Actions, condensing older Run History slightly.
- Backlog: item 8 (DeploymentItemUtility.IsInvalidPath) now DONE (PR created this run). Remaining unchanged: PrivateObject.Helpers.cs generic-method cache (net-fx only), TestExecutionManager.ParallelExecution.cs per-test array wrapping (inherent design), AggregatedConfiguration indexer scan (low impact), ServerTestHost.RequestExecution.cs Select+ToArray (per-request not per-test), RetryArtifactProcessor.cs GroupBy/Count double-enumeration (low volume, measured), RetryOrchestrator.ArtifactRecovery.cs Any() per-manifest-line (low volume) - all low priority, not fixed.
- Task schedule: Task 2 done this run (fresh scan, 1 new finding+fix), Task 3 done this run (PR created), Task 4 done this run (nothing to maintain before new PR), Task 5 done this run (nothing actionable), Task 7 done this run (issue updated, no duplication bug observed this time - monitor if it recurs). Next run: continue exploring fresh ground since today's scan proved there are still small wins available (Deployment/Configurations/Helpers areas) - consider TestDeployment.cs remaining methods, DeploymentUtility.cs (distinct from DeploymentItemUtility.cs), or Assert.ThrowsException*.cs more deeply; also watch #10914 for renewed duplication.
