<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net462</TargetFramework>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Make sure that you don't put the DLL into bin. This is expected, and will trigger a test discovery warning that we then check in parent test. -->
    <ProjectReference Include="$(RepoRoot)src\Adapter\MSTest.TestAdapter\MSTest.TestAdapter.csproj">
      <ExcludeAssets>all</ExcludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </ProjectReference>
    <ProjectReference Include="..\ClassLibForTestProjectWithBrokenTestClass\ClassLibForTestProjectWithBrokenTestClass.csproj" />
  </ItemGroup>

</Project>
