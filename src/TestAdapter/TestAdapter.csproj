<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Microsoft.VisualStudio.TestPlatform.MSTest.TestAdapter</RootNamespace>
    <AssemblyName>Microsoft.VisualStudio.TestPlatform.MSTest.TestAdapter</AssemblyName>
  </PropertyGroup>

  <PropertyGroup>
      <PackageId>MSTest.TestAdapter</PackageId>
      <PackageLicenseExpression>MIT</PackageLicenseExpression>
      <IsPackable>true</IsPackable>
      <IsShipping>true</IsShipping>
      <!-- Generate package during Build, rather than Pack, so that it can be used during Test. -->
      <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
      <Description>The adapter to discover and execute MSTest Framework based tests.

      Supported platforms:
      - .NET 4.6.2+
      - .NET Core 1.0+ (Universal Windows Apps 10+) (Visual Studio 2017)
      - .NET 5.0 (Visual Studio 2019)
      - .NET 5.0 Windows.18362+ (WinUI Desktop Apps) (Visual Studio 2019)
      - ASP.NET Core 1.0+ (Visual Studio 2017)</Description>
      <PackageTags>MSTest TestFramework TestAdapter VisualStudio UnitTest MSTestV3 Microsoft</PackageTags>

      <EnableDefaultNoneItems>false</EnableDefaultNoneItems>
      <EnableGeneratedPackageContent>false</EnableGeneratedPackageContent>
      <EmbedUntrackedSources>true</EmbedUntrackedSources>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="$(RepoRoot)src\PlatformServices.Interface\PlatformServices.Interface.csproj" PrivateAssets="all" Pack="true" />
    <ProjectReference Include="$(RepoRoot)src\PlatformServices\PlatformServices.csproj" PrivateAssets="all" Pack="true" />
    <ProjectReference Include="$(RepoRoot)src\TestFramework\TestFramework.csproj" />
  </ItemGroup>

   <ItemGroup>
    <PackageReference Include="Microsoft.TestPlatform.ObjectModel" Version="$(MicrosoftNETTestSdkVersion)" />
    <PackageReference Include="Microsoft.TestPlatform.AdapterUtilities" Version="$(MicrosoftNETTestSdkVersion)" />
  </ItemGroup>

  <ItemGroup>
    <Compile Update="Resources\Resource.Designer.cs">
      <AutoGen>True</AutoGen>
      <DesignTime>True</DesignTime>
      <DependentUpon>Resource.resx</DependentUpon>
    </Compile>
    <EmbeddedResource Update="Resources\Resource.resx">
      <Generator>ResXFileCodeGenerator</Generator>
      <LastGenOutput>Resource.Designer.cs</LastGenOutput>
      <CustomToolNamespace>Microsoft.VisualStudio.TestPlatform.MSTest.TestAdapter</CustomToolNamespace>
      <SubType>Designer</SubType>
    </EmbeddedResource>
  </ItemGroup>

</Project>
