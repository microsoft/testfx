<?xml version="1.0" encoding="utf-8" ?>
<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <!-- Import Runner.targets -->
  <Import Project="$(MSBuildProjectDirectory)/Runner/Runner.targets" Condition=" '$(EnableMSTestRunner)' == 'true' " />

  <!-- Import VSTest.targets -->
  <Import Project="$(MSBuildProjectDirectory)/VSTest/VSTest.targets" Condition=" '$(EnableMSTestRunner)' != 'true' " />

  <!-- Implicit bottom import -->
  <Import Project="Sdk.targets" Sdk="Microsoft.NET.Sdk" />
</Project>
