<Project>

  <!-- SDK top import -->
  <Import Project="Sdk.props" Sdk="MSBuild.Sdk.Extras" Condition=" '$(OS)' == 'Windows_NT' " />
  <Import Project="Sdk.props" Sdk="Microsoft.NET.Sdk" Condition=" '$(OS)' != 'Windows_NT' " />

  <PropertyGroup>
    <TargetFrameworks Condition=" '$(OS)' == 'Windows_NT' ">netstandard2.0;$(NetFrameworkMinimum);$(SupportedNetFrameworks);$(UwpMinimum);$(ModernUwpMinimum);$(WinUiMinimum)</TargetFrameworks>
    <TargetFrameworks Condition=" '$(OS)' != 'Windows_NT' ">$(SupportedNetFrameworks);netstandard2.0</TargetFrameworks>
  </PropertyGroup>

  <PropertyGroup>
    <IsPackable>true</IsPackable>
    <PackageId>MSTest.TestFramework</PackageId>
    <PackageTags>MSTest TestFramework Unittest MSTestV2 Microsoft Test Testing TDD Framework</PackageTags>
    <PackageDescription>
      MSTest is Microsoft supported Test Framework.

      This package includes the libraries for writing tests with MSTest. To ensure discovery and execution of your tests, install the MSTest.TestAdapter package.

      Supported platforms:
      - .NET 4.6.2+
      - .NET 8.0+
      - .NET 8.0 Windows.18362+ (WinUI)
      - UWP 10.0.16299
      - UWP 10.0.17763 with .NET 9+
    </PackageDescription>
    <PackageReadmeFile>PACKAGE.md</PackageReadmeFile>
    <NoWarn>$(NoWarn);NU5100</NoWarn>
    <!-- Package has non-standard lib/ layout (bundles TestFramework.dll from ProjectReference) -->
    <IncludeBuildOutput>false</IncludeBuildOutput>
  </PropertyGroup>

  <PropertyGroup>
    <RootNamespace>Microsoft.VisualStudio.TestTools.UnitTesting</RootNamespace>
    <AssemblyName>MSTest.TestFramework.Extensions</AssemblyName>
    <DefineConstants>$(DefineConstants);TRACE</DefineConstants>

    <DefineConstants>$(DefineConstants);EXCLUDE_RANGE_INDEX_POLYFILL;EXCLUDE_OS_POLYFILL</DefineConstants>
  </PropertyGroup>

  <!-- WinUI specific properties -->
  <PropertyGroup Condition=" '$(TargetFramework)' == '$(WinUiMinimum)' ">
    <DefineConstants>$(DefineConstants);WIN_UI</DefineConstants>
    <TargetPlatformVersion>10.0.18362.0</TargetPlatformVersion>
    <TargetPlatformMinVersion>10.0.17763.0</TargetPlatformMinVersion>
    <SupportedOSPlatformVersion>10.0.17763.0</SupportedOSPlatformVersion>
    <RuntimeIdentifiers>win-x86;win-x64;win-arm64</RuntimeIdentifiers>
  </PropertyGroup>
  <!-- UWP specific properties -->
  <PropertyGroup Condition=" '$(TargetFramework)' == '$(UwpMinimum)' OR '$(TargetFramework)' == '$(ModernUwpMinimum)' ">
    <DefineConstants>$(DefineConstants);WINDOWS_UWP</DefineConstants>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Condition=" '$(TargetFramework)' == '$(UwpMinimum)' " Include="$(RepoRoot)src\TestFramework\TestFramework\TestFramework.csproj" SetTargetFramework="TargetFramework=netstandard2.0" Private="True" />
    <ProjectReference Condition=" '$(TargetFramework)' != '$(UwpMinimum)' " Include="$(RepoRoot)src\TestFramework\TestFramework\TestFramework.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Condition=" '$(TargetFramework)' == '$(WinUiMinimum)' " Include="Microsoft.WindowsAppSDK" />
  </ItemGroup>

  <!--
    MSTest.Analyzers is a dependency for CONSUMERS of this NuGet package, but must NOT
    run during build of the source repo (it would flag intentional internal patterns).
    We declare it with PrivateAssets="all" to block transitive flow during build,
    then override at pack time so it appears in the output .nupkg dependency list.
  -->
  <ItemGroup>
    <PackageReference Include="MSTest.Analyzers" PrivateAssets="all" ExcludeAssets="all" />
  </ItemGroup>

  <Target Name="_UnprivatizeAnalyzersForPack" BeforeTargets="_GetPackageDependencies">
    <ItemGroup>
      <PackageReference Update="MSTest.Analyzers" PrivateAssets="none" />
    </ItemGroup>
  </Target>

  <ItemGroup>
    <Compile Include="$(RepoRoot)src/Polyfills/**/*.cs" Link="Polyfills\%(RecursiveDir)%(Filename)%(Extension)" />
  </ItemGroup>

  <ItemGroup Condition=" '$(TargetFramework)' == '$(NetFrameworkMinimum)' ">
    <Reference Include="System.Configuration" />
  </ItemGroup>

  <ItemGroup>
    <EmbeddedResource Condition=" '$(TargetFramework)' == '$(UwpMinimum)' OR '$(TargetFramework)' == '$(ModernUwpMinimum)' " Update="Properties\Extension.UWP.rd.xml" />
    <!-- API that is common to all frameworks that we build for. -->
    <AdditionalFiles Include="PublicAPI\PublicAPI.Shipped.txt" />
    <AdditionalFiles Include="PublicAPI\PublicAPI.Unshipped.txt" />
    <!-- Framework specific API -->
    <AdditionalFiles Include="PublicAPI\$(TargetFramework)\PublicAPI.Shipped.txt" />
    <AdditionalFiles Include="PublicAPI\$(TargetFramework)\PublicAPI.Unshipped.txt" />
  </ItemGroup>

  <!-- Pack: build targets -->
  <ItemGroup Label="Pack - build" Condition=" '$(OS)' == 'Windows_NT' ">
    <None Include="build\netstandard2.0\MSTest.TestFramework.targets" Pack="true" PackagePath="build\netstandard2.0" />
    <None Include="build\net462\MSTest.TestFramework.targets" Pack="true" PackagePath="build\net462" />
    <None Include="build\uap10.0\MSTest.TestFramework.targets" Pack="true" PackagePath="build\uap10.0" />
    <None Include="build\net8.0\MSTest.TestFramework.targets" Pack="true" PackagePath="build\net8.0" />
    <None Include="build\net9.0\MSTest.TestFramework.targets" Pack="true" PackagePath="build\net9.0" />
  </ItemGroup>
  <ItemGroup Label="Pack - build" Condition=" '$(OS)' != 'Windows_NT' ">
    <None Include="build\netstandard2.0\MSTest.TestFramework.targets" Pack="true" PackagePath="build\netstandard2.0" />
    <None Include="build\net8.0\MSTest.TestFramework.targets" Pack="true" PackagePath="build\net8.0" />
    <None Include="build\net9.0\MSTest.TestFramework.targets" Pack="true" PackagePath="build\net9.0" />
  </ItemGroup>

  <!-- Pack: buildTransitive targets -->
  <ItemGroup Label="Pack - buildTransitive" Condition=" '$(OS)' == 'Windows_NT' ">
    <None Include="buildTransitive\others\MSTest.TestFramework.targets" Pack="true" PackagePath="buildTransitive\netstandard2.0" />
    <None Include="buildTransitive\others\MSTest.TestFramework.targets" Pack="true" PackagePath="buildTransitive\net462" Link="buildTransitive\net462\MSTest.TestFramework.targets" />
    <None Include="buildTransitive\others\MSTest.TestFramework.targets" Pack="true" PackagePath="buildTransitive\uap10.0" Link="buildTransitive\uap10.0\MSTest.TestFramework.targets" />
    <None Include="buildTransitive\net8.0AndLater\MSTest.TestFramework.targets" Pack="true" PackagePath="buildTransitive\net8.0" />
    <None Include="buildTransitive\net8.0AndLater\MSTest.TestFramework.targets" Pack="true" PackagePath="buildTransitive\net9.0" Link="buildTransitive\net9.0\MSTest.TestFramework.targets" />
  </ItemGroup>
  <ItemGroup Label="Pack - buildTransitive" Condition=" '$(OS)' != 'Windows_NT' ">
    <None Include="buildTransitive\others\MSTest.TestFramework.targets" Pack="true" PackagePath="buildTransitive\netstandard2.0" />
    <None Include="buildTransitive\net8.0AndLater\MSTest.TestFramework.targets" Pack="true" PackagePath="buildTransitive\net8.0" />
    <None Include="buildTransitive\net8.0AndLater\MSTest.TestFramework.targets" Pack="true" PackagePath="buildTransitive\net9.0" Link="buildTransitive\net9.0\MSTest.TestFramework.targets" />
  </ItemGroup>

  <!-- Pack: Extensions DLLs into buildTransitive (net8.0+) -->
  <ItemGroup Label="Pack - Extensions" Condition=" '$(OS)' == 'Windows_NT' ">
    <None Include="$(OutputPath)net8.0\MSTest.TestFramework.Extensions.dll" Pack="true" PackagePath="buildTransitive\net8.0" />
    <None Include="$(OutputPath)net8.0\MSTest.TestFramework.Extensions.xml" Pack="true" PackagePath="buildTransitive\net8.0" />
    <None Include="$(OutputPath)net8.0-windows10.0.18362.0\MSTest.TestFramework.Extensions.dll" Pack="true" PackagePath="buildTransitive\net8.0\winui" />
    <None Include="$(OutputPath)net8.0-windows10.0.18362.0\MSTest.TestFramework.Extensions.xml" Pack="true" PackagePath="buildTransitive\net8.0\winui" />
    <None Include="$(OutputPath)net9.0\MSTest.TestFramework.Extensions.dll" Pack="true" PackagePath="buildTransitive\net9.0" />
    <None Include="$(OutputPath)net9.0\MSTest.TestFramework.Extensions.xml" Pack="true" PackagePath="buildTransitive\net9.0" />
    <None Include="$(OutputPath)net9.0-windows10.0.17763.0\MSTest.TestFramework.Extensions.dll" Pack="true" PackagePath="buildTransitive\net9.0\uwp" />
    <None Include="$(OutputPath)net9.0-windows10.0.17763.0\MSTest.TestFramework.Extensions.xml" Pack="true" PackagePath="buildTransitive\net9.0\uwp" />
  </ItemGroup>
  <ItemGroup Label="Pack - Extensions" Condition=" '$(OS)' != 'Windows_NT' ">
    <None Include="$(OutputPath)net8.0\MSTest.TestFramework.Extensions.dll" Pack="true" PackagePath="buildTransitive\net8.0" />
    <None Include="$(OutputPath)net8.0\MSTest.TestFramework.Extensions.xml" Pack="true" PackagePath="buildTransitive\net8.0" />
    <None Include="$(OutputPath)net9.0\MSTest.TestFramework.Extensions.dll" Pack="true" PackagePath="buildTransitive\net9.0" />
    <None Include="$(OutputPath)net9.0\MSTest.TestFramework.Extensions.xml" Pack="true" PackagePath="buildTransitive\net9.0" />
  </ItemGroup>

  <!-- Pack: source files (add pack metadata to existing Compile items) -->
  <ItemGroup Label="Pack - source">
    <Compile Update="@(Compile)" Pack="true" PackagePath="src/%(RecursiveDir)" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Polyfills" />
    <Using Include="$(RootNamespace).Resources" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="MSTestAdapter.PlatformServices" Key="$(VsPublicKey)" />
    <InternalsVisibleTo Include="MSTestAdapter.PlatformServices.UnitTests" Key="$(VsPublicKey)" />
  </ItemGroup>

  <!-- SDK bottom import -->
  <Import Project="Sdk.targets" Sdk="MSBuild.Sdk.Extras" Condition=" '$(OS)' == 'Windows_NT' " />
  <Import Project="Sdk.targets" Sdk="Microsoft.NET.Sdk" Condition=" '$(OS)' != 'Windows_NT' " />

</Project>
